# EHAN — Explanation-Aware Hybrid Attention Network (deepfake detection)
# Tested on Kaggle (Tesla T4, CUDA) with Python 3.10/3.11.

torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12
facenet-pytorch>=2.5.3
grad-cam>=1.4.8
scikit-image>=0.22.0
scikit-learn>=1.4.0
scipy>=1.12.0
opencv-python>=4.9.0
decord>=0.6.0
Pillow>=10.2.0
# numpy is pinned below 2.0 deliberately: numpy 2.x removed np.trapz, which the
# deletion/insertion-AUC metric relies on. The original environment kept numpy<2
# transitively (via shap/captum); pinning it explicitly preserves that.
numpy>=1.26.0,<2.0.0
tqdm>=4.66.0
tensorboard>=2.15.0
pyyaml>=6.0.1
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
